<!DOCTYPE html>
<html>
  <head>
    <title>Wizarbnb</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>


<body class="">


  <% main_background = user_signed_in? ? "background-user background-house-#{current_user.house}" : "" %>
  <div class="<%= main_background %> d-flex flex-column flex-grow-1">
    <div class="wizarbnb-navbar">
      <%= render 'shared/navbar' %>
    </div>

    <main class="flex-grow-1">
      <%= yield %>
    </main>

    <footer class="footer">
      <div class="footer-copyright">
        This footer is made with some magic at Le Wagon <i class="fa-solid fa-wand-magic-sparkles"></i>
      </div>
      <div class="footer-links">
        <a href="#"><i class="fab fa-github"></i></a>
        <a href="#"><i class="fab fa-instagram"></i></a>
        <a href="#"><i class="fab fa-facebook"></i></a>
        <a href="#"><i class="fab fa-twitter"></i></a>
        <a href="#"><i class="fab fa-linkedin"></i></a>
      </div>
    </footer>
  </div>
</body>
